# Nexus Match-Maker (Chrome Extension Scaffold)

This folder contains an initial Manifest V3 scaffold for a future Match‑Maker
extension.

- Load unpacked from `extensions/nexus-match-maker/` in `chrome://extensions`
- Popup UI lives in `popup/`
- Background service worker lives in `background/`
- Atlas SVG assets live in `assets/svg/`

